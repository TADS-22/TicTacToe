import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
    title: {
        color: '#ffffff',
        fontSize: 36,
        textAlign: 'center',
        marginTop: 24
    }
})

export default Styles